{
  "name": "canvas-cleave",
  "version": "0.0.0",
  "description": "100% JS Node.js shim for HTML5 Canvas drawImage (without resizing)",
  "main": "dist/canvas-cleave.min.js",
  "typings": "dist/canvas-cleave.d.ts",
  "tsdoc": {
    "tsdocFlavor": "AEDoc"
  },
  "module": "lib/index.js",
  "scripts": {
    "test": "jest",
    "start": "jest --watch",
    "build:lib": "rimraf lib && tsc --build config/tsconfig.lib.json",
    "build:dist": "rimraf dist && webpack && api-extractor run --config config/api-extractor.dist.json",
    "build": "npm run build:lib && npm run build:dist",
    "coverage": "jest --coverage",
    "api-extractor": "npm run build:lib && api-extractor run --local"
  },
  "author": "Daiz <daiz@limit.media>",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {},
  "devDependencies": {
    "@microsoft/api-extractor": "^7.0.17",
    "@types/jest": "^23.3.14",
    "@types/sharp": "^0.21.0",
    "awesome-typescript-loader": "^5.2.1",
    "jest": "^23.6.0",
    "prettier": "^1.15.3",
    "rimraf": "^2.6.3",
    "sharp": "^0.21.1",
    "ts-jest": "^23.10.5",
    "tslint": "^5.11.0",
    "tslint-config-prettier": "^1.17.0",
    "typescript": "^3.2.2",
    "typescript-tslint-plugin": "^0.1.2",
    "webpack": "^4.29.3",
    "webpack-cli": "^3.2.3"
  }
}
